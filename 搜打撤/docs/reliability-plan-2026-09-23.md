# 可靠性收尾计划（2026-09-23）

基线：`54042bc`。老板授权继续并行，由三个 GPT-6-luna 子代理实施，Friday 集成复核。当前 TT12、照相馆和其他会话的未提交文件不属于本轮。

| 优先级 / 负责人 | 模块和文件 | 修改前必须证明的问题 | 交付与验收 |
| --- | --- | --- | --- |
| P0 / battle_modules | `game.bag.settle.js`、`game.session.js`、`game.run.altar.js`、`game.menu.js` | 同一终局重复回调会重复发奖，或结算失败提前删除运行档；没有复现就不增加补偿机制 | 真实模块测试死亡、撤离、放弃的重复入口和失败路径；修复限定在终局逻辑。成功落盘、运行档清理和再次进入的边界必须明确。 |
| P1 / quality_gate | `game.hub.js`，必要时 `home.scene.js` | Home 尚在加载时退出/重进，以及旧场景异步命令结束后更新了新场景 | 可控异步测试证明只有当前请求挂载、旧控制器不更新新场景、加载失败后可以重试；内置浏览器检查实际基地渲染。 |
| P1 / card_replay | 新增 `tests/card-effect-semantics.test.js`，审计 fixture 仅在证据充分时修订解释 | 七条既有审计例外究竟是解析器缺口、测试环境缺口还是未实现 | 按稳定 ID 核验：`tt3sp-silverthorn`、`tt7-holyheal`、`tt7-meteorstrong`、`tt8-hero-sealer`、`cc-cursed-blade`、`cc-ember-burst`、`cc-mana-surge`。玩法源文件有其他改动，发现错误先报复现与最小修复方案。 |
| 集成 / Friday | 本计划、相关验收记录、隔离 Vite 验证目录 | 工作区结果不能代表独立工程提交，代理单测不能代表完整游玩 | 审查归属与 diff，定向回归；公共层变化才扩大测试。生产构建在隔离目录执行，沿用原性能预算。实机与未验证项分别记录。 |

## 完成边界

- 不更改卡牌数值、掉落规则、收费规则来让用例通过；不添加通用事务框架。
- 若存储层吞掉错误、无法证明提交成功，应记录限制并缩小修复声明，不能用 mock 抛错证明真实落盘可靠。
- 终局恢复的自动测试、生产 UI 路径、四层完整通关是不同证据，不互相替代。
- 只提交本轮可分离的修改；不推送。不以本轮通过宣称商业发行的所有条件已满足。

## 执行记录

- 三个既有子代理均已进入 running；不再新增会话以绕过并行限制。
- Home 旧异步命令更新新控制器已被定向用例复现，正在限定修复范围。
- 卡牌语义提交 `30081cd`：主工作区与隔离基线均通过 7 个文件 / 50 项定向测试。新增测试的等待超时改为显式失败、逃离后排空队列，最后两处各重跑新增文件 3/3 通过。

### 卡牌语义证据

| 卡牌 ID | 行为证据 | 审计例外的实际性质 |
| --- | --- | --- |
| `tt3sp-silverthorn` | `card-effect-semantics.test.js`：选择燃料、确认注能、出牌，敌人 999→991 | 真实战斗支持增伤；逐句审计没有注能上下文 |
| `tt7-holyheal` | 同上：使用费用 2 燃料，生命 50→54 | 需要真实燃料费用 |
| `tt7-meteorstrong` | 同上：注能两张，三次火球共 12 伤 | 战斗步骤支持重复施法，逐句审计缺少上下文 |
| `tt8-hero-sealer` | `abyss-sovereign.test.js`：BOSS 开局入牌库，集齐封印后化形；普通战对照 | 属于战斗开始流程 |
| `cc-cursed-blade` | `cursed-blade.test.js`：读取真实手牌，8 伤及中毒 2 / 灼烧 1 / 破甲 2，无诅咒对照 | 审计占位手牌没有诅咒 |
| `cc-ember-burst` | `ember-burst-rot-seed.test.js`：击杀 A 后 B 受到两轮各 4 伤；未击杀对照 | 属于命中后击杀连锁 |
| `cc-mana-surge` | `mana-surge.test.js`：固定测试随机种子，四个施法序号、法术池成员、实际效果与正常收尾 | 属于专用施法流程 |

这七项均不是“已知但忽略的未实现玩法”。审计仍须拒绝新未知句式和过期例外。尚未穷举星陨每发法伤加成/中途击杀，以及法力奔涌所有候选法术的默认注能副作用。

### 终局和基地交付

- `77be09a`：Home 的旧操作入口不再提交命令；已发出的命令成功后只刷新所属代际的场景，保留成功结果。3 项测试覆盖退出时尚未完成的导入、退出后重进、旧操作迟到、挂载失败后重试。调试阶段的导入等待问题已收敛，Friday 11:52 对最终文件复跑 3/3 通过，随后隔离全量中该文件也通过。
- `461cf76`：`doDeath`、`abandonRun`、`doExtract` 在局已结束时拒绝再次结算。新增 2 个文件 / 4 项通过；旧撤离和菜单 2 个文件 / 8 项通过。死亡测试调用真实 Base 入库，同局重复后卡数为 1；重新开启 runActive 的新局状态后卡数为 2。
- 战后 `WeakSet(opts)` 初版已撤回：在异步过场前登记完成会阻断失败后的重试，本轮没有把这段实现纳入提交。
- 存储失败的终局恢复**未实现**：旧 `Base.deposit/depositCards` 不向调用方传递保存失败，不能声称运行档与基地存档已原子提交。上述守卫只防同一运行状态的重复入口，不解决跨刷新和部分写入失败。

### 隔离产物与实机验收

验证目录为 `%TEMP%/soudache-release-20260923/搜打撤`，基于前轮已验证 `89e46fa`，复制本轮 10 个明确文件并逐一比对内容一致；其源码与本轮提交 `461cf76` 对齐，生成清单由构建再生成。没有复制主工作区的 TT12、照相馆和 UI 改动。

- 生产目录 `desktop-app/game-reliability-461cf76`，构建和原性能预算通过：包体 61.14 MiB、重复资源 0.52 MiB、入口 gzip 186.1 KiB、首屏 JS gzip 227.8 KiB、战斗 84.1 KiB、叙事 31.0 KiB、CSS 84.4 KiB。语法 148 个文件通过。日志 `%TEMP%/soudache-reliability-build-20260923.log`、`soudache-reliability-syntax-20260923.log`。
- 内置浏览器 `127.0.0.1:5198`、仅本任务测试档位 02：恢复遭遇战检查点→撤离判负→失败结算→回基地；检查实际 Pixi 场景渲染、返回标题、刷新，档位 02 不再显示继续旧战斗。
- 同一测试档重新整备→确认角色→领取两张职业卡→进入地图→主动放弃→回主菜单→刷新，档位 02 显示进入基地；未捕获浏览器 error。原有档位 01/05 未进入或覆盖。
- UI 验收中的安全格为空；安全卡入库数量由真实 Base 行为测试验证。没有把这次判负操作写成生命归零全流程验收，也没有验证四层成功撤离、长期内存/帧率和真实存储配额失败恢复。
- 隔离全量 `npm test`：113 个文件、632 项中 **630 通过 / 2 失败**（11:53:47 开始、309.90 秒），失败集中于旧 `extraction-flow.test.js` 的源码切片执行器不支持新导出的 `doExtract`，并非断言结果不同。修复 `9760337` 只补齐该执行器的导出语法转换，不改断言与生产代码；随后主工作区和隔离目录各跑撤离流程、撤离结算、终局死亡、终局结算，均 **4 个文件 / 10 项通过**。日志 `%TEMP%/soudache-reliability-full-20260923.log` 保留首次失败，不改写为全绿；修复后未重复运行全套，不能称最终全套单次 632/632 通过。
- 临时浏览器页与预览服务器已关闭；隔离产物保留。仅本地提交，未推送。其他会话的未提交改动仍保留，包括本轮期间新增的 `ui.js` / `boot.test.js` 修改。

### 下一切片的精确边界

1. `game.session.js` / `game.menu.js`：先迁移死亡与放弃到已有 `recovery.commands.js:commitBaseAndRun`，保留当前掉落规则；验证基地写失败、运行档删除失败、重启恢复和同一请求重放。
2. `game.run.altar.js`：撤离整理须有可恢复的待入库状态，确保刷新时不丢未整理卡牌且不重新掷口袋保留概率；复用已有 `recoverable-pair.js`，不另建事务框架。
3. `game.bag.settle.js`：区分结算进行中、奖励已提交、演出已结束；以真实过场失败和部分奖励写入失败测试定义重试行为，再实现去重。不得用一个永久布尔值替代这些状态。

以上三项是后续计划，并非本轮已完成内容。
