# Vite 冬日远征改造 · 接手 AI 交接

2026-09-12，老板要求「提交改动，停止项目，总结交给其他 AI 的规划」。已停止继续开发和生图；本文件是工作暂停点，不是最终发布验收。

## 范围与审美要求

- 只开发 `D:\素材\代号柒\搜打撤` 的 Vite 版；Godot 冻结，不动 Godot，不打包 EXE。
- 用户希望全面、大刀阔斧的改造，UI、地图、商店、事件、人物、卡面、音效、动画都可调整。不要退回零碎修边框的方式。
- 视觉参考明日方舟与集成战略的信息层级、战术版式、画面留白；本项目使用冬日蓝黑、霜白、香槟金，危险用珊瑚红。官方参考：https://ak.hypergryph.com/is/gardenofgrotesqueries?source_from=official
- 战斗可参考杀戮尖塔 2 的操作与分区设计。用户说文件夹里有相关代码，但本轮没有定位到可确认的参考源码，不能宣称已移植该游戏代码。
- 无（`wu` / 侠客）的人设：蓝眼睛；有刘海但不遮眼；白色长发、高马尾与蓝缎带；黑礼裙；可戴墨镜，通常在头顶；神态嚣张、自信。共同参照首页壁纸与已有定稿立绘，不要每张卡随机改变脸、眼色和发型。
- 默认中文，称呼老板；Friday 是本轮协调者。可用多个 Luna 子代理分工，但修改文件所有权必须明确。

## 已落地的基础

| 模块 | 主要文件 | 当前成果 |
| --- | --- | --- |
| 视觉体系 | `game/css/expedition*.css`, `game/index.html` | 标题、基地、整备、卡牌库、人物、商店、事件、战利品、战斗独立样式层；入口已挂载新 CSS |
| 地图 | `renderer.js`, `renderer.icons.js`, `camera.js`, `expedition.view.js` | 已知范围镜头、合法路线按钮、章节信息、节点标签；最终一批已换成小型菱形与线性战术图标；去掉旧整图大缓存 |
| 局外 | `game.hub.js`, `game.cardslib.js`, `game.run.altar.js` | 出发简报、真实整备数值、纵向基地导航、牌库多条件筛选/排序/详情、选角默认预览 |
| 战斗 | `battle.view.js`, `battle.feedback.js`, `battle.frames.js` | 点击选牌后点目标与拖拽共存，1–9 选牌，Esc 取消，目标预览，明确回合状态，独立战术按钮区 |
| 事件/商店 | `game.run.scenes.js`, `game.run.shop.js`, `chests.js` | 事件右侧选择面板、战利品容量信息，战术补给站/收购台、购买反馈与库存状态 |
| 声音 | `sound.js`, `sound.scape.js`, `sound.policy.js` | 标题/探索/战斗合成声景、原曲可切换、音量/静音/隐藏暂停、压限、重复播放节流 |
| 修复 | 多文件及对应测试 | 小地图隐藏时尺寸错误、敌人抠图挂载时序、治疗误触红闪、战斗结束动画立绘残留、紧急撤离不足 3 卡仍可点等 |

### 新美术

- `game/assets/scenes/winter-expedition/`：四层背景 + 普通/首脑战场，共 6 张。
- `game/assets/cards/expedition-starters/`：初始攻击、火球、穿刺、法杖、木材、口粮、无的 3 张能力卡，共 9 张。以 `art-mapping.json` 的 `cardArtOverrides` 为最终命中入口。
- 无的 6 张人物卡面已按蓝眼、露眼刘海、墨镜、嚣张神态重绘并由根代理检查。早期错误眼色的 `cards/wu-*.webp` 仍留在旧路径，但三个能力 ID 已优先映射到 `expedition-starters/wu-*.webp`；后续可清理冗余，切勿改回旧图。
- 玄砾 `heixiang`、灯葵 `xingyue`：已补 `portraits/full`、`avatars`、`battle` 三组映射。初版 full 与 avatar 已看图；battle 初版不透明，最后一批正在替换透明战斗图时停止。当前 battle 文件最后写入时间约 19:42–19:43，必须检查 alpha 和实机效果，不能把「生成成功」当作透明素材验收成功。
- 生图管线说明：`C:\Users\太平\.zcode\cli\memories\projects\project-9f3e184c77472436\memory\soudache-nai-card-pipeline.md`。遵守 NAI 单队列和间隔，不并发生图，不输出或提交 token。
- 提示词/原图/manifest 保存在仓库根 `.tmp/nai/friday-*20260912/`，属于本地工作记录，不在本次 Git 提交中。

## 验证的准确边界

- 最近一次全量：Vitest **42 文件、270 测试通过**；`node game/selftest.js` 和 `node scripts/validate-data.mjs` 通过。对应日志 `.tmp-vitest-final.log`（本地忽略文件）。
- 此后最后一批地图又通过 14 项相关测试；商店修改通过 `boot.test.js` 14 项；选角通过 19 项相关测试。
- **最终 Git 快照仍需重新完整 build/test**：最后的 CSS 接入、卡框优先级和手牌抬高改动之后，用户要求停止，因此没有继续构建或回归。已有 `desktop-app/game` 产物不能当作最新快照。
- 根代理实际试玩了标题、档案、基地到出发、人物选择、路线、修鞋铺/救济站/营火、物资选择、商店购买、普通战斗进入；看过牌库整页和新美术。
- 战斗实测点击初始攻击后可选中，提示目标；Esc 可取消且 `aria-pressed` 归零，没有额外退出弹窗。新点击命中敌人的行为有真实 view/commands 集成测试通过，但最后实机点击命中过程被页面刷新打断，尚未走完。
- 早期原版拖拽战斗已胜利；后续改版需要重新验证拖空回手、点击/拖拽不重复消费、回合结束、胜利掉落与动画清场。
- 浏览器实际 DOM 尺寸为 1440×900。工具设置 1280×720 后 DOM 仍报告 1440×900，因此**不能宣称已完成小屏实机验收**。临时 viewport 覆盖已 reset。
- 未人工听音，未完整 Boss 通关/长局平衡验收。

## 接手后优先顺序

### P0：先完成最新版本验收和明显缺陷

1. 启动单个 Vite 服务，核对打开的 URL 与该进程。此前同时存在 5175 与 5176，旧 5175 的 HMR 通道可能使 5176 页面随编辑重载。不要再让多个服务互相干扰验收。
2. 运行 `npx vitest run`、`node game/selftest.js`、`npm run build`；esbuild 若遇 Windows sandbox 的 spawn EPERM，使用获准的本机执行方式，不修改全局配置。
3. 重看最新地图、基地、整备、商店、选角、牌库、卡面详情。历史 `winter.css` 有大量高优先级覆盖，不能只看新 CSS 文件就宣布有效。
4. 实战排版：最终 `expedition-battle.css` 将手牌 `.bt-slot` 从 `bottom:-28px` 改为 `18px`，我方/敌方整体底边改为 `60px`；这解决此前底部裁切的方向正确，但**最后这组值尚未实机复验**。检查手牌不遮血条、名字、战场目标，并在 720 高度适配角色尺寸。
5. 检查 `heixiang/xingyue` 的 battle WebP 透明通道与底部脚是否完整；若仍有背景矩形，需要补完已有离线抠图流程。
6. 检查商店 h2 是否仍被旧 `display:none` 隐藏、价格按钮是否有清晰卡名语义、购买反馈与可离开按钮在溢出时是否可见；不要让三列大格只有中央一小张卡造成空洞。
7. 选角目前是默认「预览」而非已选中，首次确认仍禁用且要点头像。可简化为本地默认选择（仅确认时写游戏状态）。新按钮与头像条有重叠风险，尤其 820 宽。
8. 核对战斗选中状态在换牌、拖拽、翻页、回合切换和关闭时是否一致；旧目标/费用提示文案有残留，统一为真实支持的操作。

### P1：下一轮真正可感知的体验提升

- 地图：在当前几何节点基础上做清晰的分支构图、节点类别图例与风险/收益预览；镜头移动与当前节点反馈统一节奏。先试玩找到看不清的原因，别反复堆光效。
- 战斗：伤害预览使用真实结算口径；增加可取消选牌的稳定状态与小幅攻击位移、受击、格挡、死亡节奏，统一时间轴；任何动效都不能挡住选择或多消费卡。
- 事件：补剧情气氛、明确选择代价和收益、奖励过渡，不添加与实际规则不符的假数值。
- 美术：当前审计约 **89 张卡使用通用家族图**，不是加载缺失。按高频/职业核心/商店出现率分批换，参考 `art-coverage-2026-09-12.md` 前 12 张名单。
- 人物：将同一份人设参照用于头像、战斗、卡面和立绘；新增两位暂时只有静态资源，不要误称已有完整逐帧动画。
- 声音：实际试听各总线音量与切换，检查长期循环是否疲劳；切来源、隐藏窗口和静音时不重复启动音乐。

### P2：内容与长期基础

- 全角色长局、Boss 牌库、死亡/撤离/读档回归与平衡。
- 宝藏大门特殊关卡仍未制作，不要把装饰入口当完成功能。
- 整理 `winter.css` 历史覆盖为清晰模块，减少 `!important` 叠层；先建立截图基准后再清理，避免样式回归。
- 补自动化关键操作的集成覆盖，保存确定性随机和存档兼容；不要重排 `main.js` boot 导入、让 session 反向依赖 hub/run。

## 停止状态与存档

- 本轮启动的 Vite 5175 / 5176 服务已停止，三个子代理已中断/结束；不会继续自动开发。
- 本轮浏览器参考页已关闭，游戏页可能仍显示最后页面，但服务已停。
- 5176 是本轮验收来源。档位 1 与 3 是根代理使用的测试进度；档位 2 在本轮已存在且来源不确定，不要覆盖。档位 3 约在第一层第 7 个节点，使用无，普通射手遭遇战，适合接着验收。
- 本次提交保存 Vite 当前工作区快照，包含此前已有但未提交的 Vite 卡面/战斗工作；Godot 与根 AGENTS.md 的既有变更不纳入该提交，也不回滚。
- 不修改全局 `config.toml`，不提交临时日志、NAI 凭据或本地原图工作目录。
