# R5-b 初轮根审：样本特例退回

实施草稿曾按 `r5-route-a` / `r5-route-b` 硬编码起点与 donor，以匹配设计矩阵。根任务在交付前阅读发现，明确拒绝。验收种子只能提供重复证据，不能成为生产分支的选择条件。

修订：删除样本特例，所有 seed 走同一有界通用算法；重新打印两固定种子的真实矩阵，如与原方案不同则更新方案并解释。不能为保持文档答案反向定制实现。

另一个质量门：chest 与 chest 交换没有实际类型变化，不算新内容 applied。必须验证交换前后真实类型不同，且最终分支对应正式物资/战斗流程。没有合格候选就回退原图，记录原因。

原方案中的固定seed实机注入与临时seed控件已撤销。最终证据分开：固定样本自动化验证；正式随机新局IAB实际分支与刷新；旧v1局仍用原127来源QA05保留对照。新测试来源为同服务器localhost:4179，五档空白已由root页面确认。

玩家名称改为“补给支路 / 交战支路”，因为两支同为两步，不暗示路线长度不同。此包只做L2局部分岔，不宣称已经实现两套全局路线或多结局地图。
