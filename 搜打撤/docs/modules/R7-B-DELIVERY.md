# R7-b 本地试玩反馈交付

- 右键反馈保留正文必填，并增加可选复现步骤、期望和实际；记录当前版本、中文页面、可读位置和百分比坐标。
- 浏览器只写 `sdt-suggestions-v1`；成功文案明确需在设置 → 留言库主动导出单条 JSON 交给制作者，不自动发送。
- 单条导出按留言数组索引定位，重复时间戳不会导错；导出白名单排除 selector、URL、未知字段和存档，Blob URL 会回收。
- 坏 JSON/非数组原串保留并显示错误；写失败保留全部输入，不定时关闭；保存 busy 防双写，旧留言不迁移回写。
- 保留现有 Electron IPC 透传分支，未修改任何桌面文件。

UI 路径：任意游戏页面右键 → 填写并保存 → 设置 → 查看历史留言 → 导出此条。

实机布局、键盘焦点和真实下载仍由根在 IAB 验收；自动测试不冒充实机通过。

根最终补记（06:55）：已完成IAB 1280×720、960×540滚动和焦点检查、真实四字段保存→留言库→刷新保留→下载JSON磁盘读取。完整设置/留言库往返后页名已修正并实机复验。R7-b通过；完整证据及全项目仍存红灯见R7-FINAL-VALIDATION-2026-09-21.md，本文早期“仍由根验收”至此已收口。

定向命令：`npx vitest run tests/r7b-feedback.test.js tests/r5a-session-menu.test.js --no-file-parallelism`。

最终收口命令覆盖反馈、连续射击、R3-b、菜单与启动链共 5 个文件、42 项全部通过；其中 R7-b 真实控制器/纯模块 14 项。新增模块与菜单通过 Acorn 语法检查，定向 `git diff --check` 通过。

返工覆盖：storage getter/读写异常恢复、延迟 IPC 保存取消后重开、坏 JSON 与非数组 DOM 错误、旧记录不回写、四字段 HTML 文本化、重复时间戳按索引导出，以及下载点击异常后的 URL 回收。
