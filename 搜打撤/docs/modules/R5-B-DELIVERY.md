# R5-b 路线覆盖交付

## 行为

新普通远征在 L2 尝试一次确定性路线覆盖：同一起点可见“补给支路 · 物资点”和“交战支路 · 遭遇战”，两步后汇合。覆盖只交换现有普通节点完整 def，不改节点、边、层数、设施、配额、奖励来源或撤离费用。失败保留原 3/9 图，并在 v2 `routePlan` 记录 `NO_MUTABLE_BRANCH_PAIR`、`QUALITY_GATE_REJECTED` 或 `OVERLAY_EXCEPTION`；回退不显示路线标签。

固定样本均走通用算法：`r5-route-a` 为 N3 起点、N5 汇合、N6 补给、N4 交战，N6 与 N9 交换；`r5-route-b` 为 N7 起点、N9 汇合、N8 补给、N10 交战，N8 与 N3 交换。生产逻辑没有 seed 特例或隐藏 seed 接口。

## 验证

定向测试覆盖通用算法两张矩阵、100 seed 确定性扫描、原生成质量门、战斗节点同层战斗邻接数不超过 1、配额不变、v1/v2 严格恢复、损坏 routePlan 拒绝、真实 session 既有保存恢复和路线视图转义。100 seed 当前结果为 63 applied / 37 fallback（9 `NO_MUTABLE_BRANCH_PAIR`，28 `QUALITY_GATE_REJECTED`），没有为应用率放宽质量门。

现有开发者控制台增加一行只读路线身份，显示 mapSeed、snapshot/routeVersion、routePlan.status 与 fallbackReason；值经 HTML 转义，不含 seed 输入或重生成动作，默认玩家界面不显示。

## Friday 实机验收（2026-09-21 05:33）

在 `localhost:4179` 原先五个空档的来源，以正常菜单创建 QA05 黑像新局。只读控制台确认 mapSeed=344527573、snapshot v2、routeVersion=r5-routes-v1、applied；没有指定种子、赠资源、跳战斗或强制胜利。L2 节点7可见交战支路8、补给支路12，两支汇于火堆9。

实际收取12补给箱（木材、宠物蛋、1币）后刷新续局：位置12、21/35生命、行动9、1币、背包8/16及两个相邻节点保持一致，没有重开箱。经火堆9到交战8，以正常两张初始攻击及两次砸击完成两名机动兵战斗，19/35生命，正常收绿化箭与1币。回起点7，两支均正确显示已探索、不再刷新，截图文字完整。测试局保留在起点7，行动12、2币、背包8/16；开发者设置恢复OFF。

原 `127.0.0.1:4179` QA05 的旧v1火堆局在新代码下正常续局，35/35生命、行动1、背包3/16及相邻入口1/物资3/战斗5不变，五个可见节点和连边仍为旧图；01—04未进入或修改。两固定seed属于自动化证据，实机仅这场自然随机图，不混称固定seed实测。

根判定：R5-b有限范围通过。完整两条大型路线、多结局、全部随机图真人趣味性不在本包完成声明内。100种子37%回退仍明确记录；回退不强造路线。
