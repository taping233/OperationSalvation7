# R1-b 房间候选背景接入

## 可见变化与边界

将旧透视背景+纯色悬空地板，替换为冷青墙面、琥珀壁灯、雪窗和完整石板地面的等距房间候选。已有家具、人物、展品实际站在该图地板上；仍是候选美术，不冒称老板已认可。家具保持六个 clean SVG，不扩展家具数量、养成规则或经济。

## 输入输出与范围

输入：`.tmp/nai/output/r1-room-direction-20260921-024648/room-direction.png`，1216×832；原图、brief、request保留。由官方painterly模块v1.0+NAI V5生成；种子731942861，28步单样本。输出：新增游戏资产原样复制、来源清单、home.visuals中带尺寸/地面标定的记录，home.scene统一背景contain与坐标正逆映射。允许修改这两个模块、home-scene.css、相关M04/M05测试和本包交付文档；不改存档/购买/角色/出征接口。

参考标定：图片地板后端约(619,280)，左端约(174,535)，右端约(1120,494)，前端约(628,749)。可用略向内收的平行四边形避免不规则边缘：原点(628,330)，每列向量(53.5,23)，每行向量(-70.7,29.7)，8列6行。不要拉伸位图去凑旧投影；以原图contain后的同一变换驱动Pixi、DOM展品/人物、48格与逆映射。可调整标定但记录理由。

普通模式不盖不透明棋盘在石板上，编辑模式画轻网格。足底锚点和遮挡排序保留；人物不挡窗及热点，保持合理房间比例。展品用现有真卡但填满展柜合理面积，完整卡面和下方名称可辨，不再缩成柜中央的小邮票。WebGL不可用保留DOM人物与候选背景；缺候选资源则回退原型而不阻断操作。

## 验收

- 1280×720、1920×1080：房间比例不变，完整地板可见；人物和已保存椅子落地；五热点不叠人物/展品。
- 编辑前后图/格/家具共享变换，非边缘格放置与取消正确，越界拒绝；收起后不可聚焦。
- 正反投影样本包含四边和内部点，48格不越出可用地板；背景缺失时仍可操作。
- 第五档现有10币、chair-1(1,3)、未放置chair-2与table-lamp-1保持；不可覆盖01—04。
- 只允许这张现有小稿，不再生图、不出2048正式图、不新增计费。

停止点：交真实改动、定向测试、尚未实机项；Friday根IAB两尺寸复验后才能接R2-a。
