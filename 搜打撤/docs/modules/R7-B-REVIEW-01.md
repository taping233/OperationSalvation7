# R7-b 根审返工 01

Friday读取真实控制器与5项本包测试后，不接受首轮“迟到回调隔离已完成”的结论。代理报9/9包含另一文件的4项，不是本包9条。

## 明确问题

1. `readLocalFeedback(storage = localStorage)`在try外访问存储属性；SecurityError可越过Result，保存控制器缺catch/finally，按钮可能永久busy。
2. 取消不失效open token，新开不重置disabled；旧异步保存取消重开后，旧回调因token不同直接返回，新层保存按钮仍禁用。需要真实延迟Promise测试，不用源码断言替代。
3. 下载click抛出时未回收Blob URL、未显示失败重试状态。
4. 留言库没有呈现新增三个字段与版本；可读位置仍显示selector代码。
5. 测试每轮注册document监听而未移除，且esc:String不能证明HTML转义；缺坏数据真实DOM、旧记录读取不回写、取消重开和异常存储获取证据。

## 有限修订与停止点

保持R7-b原文件与产品范围，修存储Result边界、弹层生命周期、可重试导出及四字段可读展示；补真实控制器事件测试及监听隔离。Sol完成后停止修改，由Friday复核代码、亲跑定向、在IAB走保存→读取→刷新→下载和两尺寸可达性。尚未通过前不计入完成列表，不扩展网络反馈平台。

## 二审补缺

首轮返工已修按钮重开、存储getter和四字段展示。仍发现测试监听未清理，后续右键可能由首个旧控制器处理；下载createObjectURL仍在try外。最后小修限定为测试监听隔离、当前控制器state断言、下载全段异常Result及真实控制器失败→重试、坏存储保存保持、四字段转义完整断言。其余不扩展。
