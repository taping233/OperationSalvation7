# R1-b 候选房间背景整合交付

## 编码前实现简报

- 候选图保持 1216×832 原始像素和宽高比，原样复制进 `game/assets/home-prototype/`；不修改光栅、不重新生成、不产生费用。它是待老板审美确认的小稿，不标记为最终美术。
- VisualPack 记录原图尺寸、来源、种子和地板标定：原点 `(628,330)`、列向量 `(53.5,23)`、行向量 `(-70.7,29.7)`，覆盖 8×6 的内收可用区域。
- `createHomeProjection` 先把整图 contain 到场景可用区域，再把原图标定点和两个基向量映射到屏幕。`projectHomePoint` 与 `unprojectHomePoint` 使用同一 2×2 仿射矩阵；Pixi 家具/人物、DOM 展位、48 格按钮和降级背景共用该结果。
- 普通模式只画候选房间，不覆盖不透明棋盘。编辑模式由 48 个轻量命中格显示网格。候选资源不可用时保留暗色背景和操作能力。
- 展柜继续消费真实 `.hs-card`，在独立展柜区域内提高缩放并保持完整卡面和下方中文名，不修改全局卡样式。

## 允许文件

`home.scene.js`、`home.visuals.js`、`home-scene.css`、M04/M05 对应测试、本文件，以及原样复制的候选 PNG。经济、存档、角色、出征接口不改。

## 实际交付

- 原图已原样复制到 `game/assets/home-prototype/room-direction-candidate.png`，1216×832、1,031,685 bytes、SHA-256 `66E3BB17DC7EEF8424E08551F33C0EFD395B248FC8011172D14F3B08223CB747`。源文件、brief 和 request 未修改；本次没有新增生成或额外 Anlas 支出，既有账单仍为 8195。
- VisualPack 新增候选资产与完整来源信息，版本标为 `r1b-room-candidate-1`、状态为 candidate。标定沿用计划值，因为查看原图后确认四角都落在石板边沿内侧，无需为不规则外缘扩大操作区。
- `createHomeProjection` 将候选图 contain 到当前可用舞台，再缩放原点和两个基向量；正投影和 2×2 矩阵逆投影共享同一数据。编辑侧栏打开时，图片、人物、家具、展位和 48 格一起缩入剩余宽度。
- 删除旧 supply-room 运行时背景和 Pixi 不透明地板格。普通模式只显示候选房间；编辑模式仅由半透明 DOM 格显示标定网格。Pixi 初始化失败时使用同一 candidate URL、contain 矩形和坐标的 DOM 背景，缺资源时暗色舞台仍可操作。
- 左右展位重新落在地板两端；真实 `.hs-card` 缩放从 `.3` 提高到 `.68`，在 104×118 展柜内接近完整可用面积，小屏为 `.52`，下方中文名保持独立可读。规则仍限定在 home 展柜。

## 验证

- 投影测试覆盖 1280×720、1920×1080 的四边角和内部点：`grid → screen → grid` 往返一致，所有样本位于 contain 图矩形内。
- VisualPack 测试验证 candidate 状态、1216×832 尺寸、原点和两个标定向量，以及候选文件实际存在。
- M04/M05 定向 3 文件 15 项通过；M01–M06 回归 9 文件 42 项通过；`node --check` 与本包 `git diff --check` 通过。
- 候选审美、两尺寸最终构图、重载后的 chair-1 `(1,3)` 仍需 Friday 根 IAB 复验；本包不把候选图或六件原型家具称为最终美术。

## 根 1280 验收后的两项修正

- 根实测真卡高度 181.36px、展柜有效高度 117.292px。桌面缩放由 `.68` 调整为 `.60`，卡高约 108.82px，上下合计保留约 8.47px；小屏展柜 92px 使用 `.48`，卡高约 87.05px，保留约 4.95px。费用和底部币不再贴裁切边。
- 候选 asset spec 缺失时立即显示可见原型地面和错误 notice。asset 存在时，隐藏 DOM `<img>` 作为真实加载探针；异步 `error` 同样切换原型地面。回退只改变背景，不禁用热点、编辑器或命令。
- 新增真实 `error` 事件测试：派发候选图片加载失败后验证原型地面可见、错误提示出现、热点仍发 `openPanel`。定向 3 文件 16 项通过，M01–M06 回归 9 文件 43 项通过。
