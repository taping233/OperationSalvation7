# R3-b 出牌边界与表现事件（已批准实施）

实施状态（2026-09-21）：重复命令已在普通/BOSS 真实 commands 中复现并完成批准的最小修复，等待根验收。文件范围锁定为 `game/src/battle.core.js`、`tests/r3-b-action-boundary.repro.test.js` 与 R3-b 文档；未修改 R4、effect-steps、卡库、存档或视图。

R3-0只修双镖和砸击。根静读发现 `play()` 查findCard但不查hand，findCard仍能找已打出实体；queueCardExecution直接扣费移牌。不能因此直接断言所有UI会重入，但M07-C必须用实际commands验证迟到的同uid调用。

先复现：普通/BOSS各一次合法初始攻击，等queue稳定后再调用相同uid；期望不再次扣费/伤害/移动卡。测试合法“保留在手牌”、再次抽回同一uid可重新使用、freeCast临时卡尚在等待目标等例外，不能简单把所有见过uid永久拉黑。只有复现后才修最小入口守卫。

若为M08提供表现ID，只在真实提交的出牌事件附加当前battle唯一、随每次合法行动递增的presentation receipt；不保存动画中间态、不靠动画回调伤害。不得以卡uid当全场唯一行动ID（同卡可重抽）。

输入当前会话命令/uid/目标，输出现有权威结算和至多一份动作表现事件；范围core局部边界、针对性测试、必要view只读消费。不做新的分布式事务、不重写effect-steps或战斗队列。

正常：普通/首脑各一次，重复/取消后迟到无额外结算，重新合法入手可再用。失败：非法uid/无手牌/能量不足不写规则；免费释放的实际费用与预览一致。重试：跳过/关闭/倍率改变仅影响表现，相同起点最终规则一致。

停止点：先交复现结果，根批准后最小修复，再根IAB验证。若代码证据不成立记录解释不硬造问题；R3-a完成以前不实施。
