# R1-a 二次根验收：交互通过，显示细节返工

2026-09-21，实际 IAB 1280×720，档位 05。

## 已验证

- 新地面/人物落点与五入口布局可理解，编辑面板按钮水平可读，结束了上一版悬空背景与中央热点堆叠问题。
- 拿起 chair-1 从 (3,2) 移到 (1,3)，实际 Pixi 家具跟随，未保存状态可见。
- 草稿期间购买灯：币 17→13，chair-1 草稿仍在 (1,3)，不被购买更新吞掉。
- 灯放 (2,4) 无桌支撑：显示“桌面摆件需要工作桌支撑”，保存禁用。取消恢复 chair-1 原保存点 (3,2)。
- 灯选中后点击地面外 (225,550)：提示“地面外不会自动吸附”，已放物件不变。
- 另买座椅：13→10，chair-2 重叠 chair-1 时明确重叠提示且保存禁用。
- 取消后把 chair-1 放 (1,3)，保存成功且状态变回与存档一致。重载验收待最终显示修复后执行，避免在 HMR 中重复导航。

## 需修显示与可访问性

1. 真实 `Cards.cardHTML` 的根类是 `.hs-card.tp0.rv6.cf1.sm`，不是 `.card`。home 样式针对 `.card` 的缩放不生效，卡片顶部标题和底部数值被展柜框截断。请消费真实组件或提供独立紧凑展品（卡面+中文名），不可只测试假的 `.card` fixture。不要改全局卡牌样式。
2. chair.svg 的三条腿到 y=190/203，text 在 y=207；通用底部13% mask把腿一起截掉。请为本包创建无调试文字的同源 SVG 变体，完整保留形状，使用真实脚底pivot；保留原样张源文件与provenance，不再以crop比例破坏物件外形。其他家具也逐个检查。此为项目原生可编辑 SVG，允许修改/派生，不涉及栅格图片编辑。
3. 网格48个button均无名称，title被全局删除。添加简短中文 aria-label（地面列/行、墙格、已有家具名），满足键盘/可访问性验收；隐藏编辑器不要让其按钮仍可tab聚焦。

当前是交互功能已实机通过、显示细节未过，不是 R1 最终视觉通过。六家具与地面仍是设计原型；正式图画继续单列，不能以本次地面调整标为最终美术。

停止点：仅修三项，定向验证真实 `.hs-card`、无裁腿资产、grid命名与隐藏焦点，再交根任务两分辨率/重载确认。
