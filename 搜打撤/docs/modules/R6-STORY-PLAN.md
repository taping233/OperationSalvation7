# R6 环境短剧情（预备，未派发）

## 可见目标

用一条三段环境线索、两个阶段结局支撑“选择”，不替五名角色补写尚未确认的性格和关系。最终内部批准稿见 `R6-A-LAST-LAMP-DESIGN.md`：发现旧路标→查明指向冲突→同一灯具开放远射或遮光近照。两种设置自然互斥，永久纪念只是见闻与选择记录，不产生物资代价、奖励、地图或战力效果。

这只是叙事方向草案，不是老板定稿。实施前先读现有 `narrative/events.ink`、`narrative.js` 和实际事件触发器，提交一页文本与后果矩阵给 Friday 内部审阅。新内容先限定环境描述，不使用角色口头禅或追加角色背景。

## 输入输出与分包

R6-a：纯故事状态机+Ink内容，输入事件实例ID、选择、基地故事快照、本局状态和节点上下文，输出合法选项、StoryReceipt、下个事件条件、可展示纪念物ID。不执行JS字符串、不直接发卡。

R6-b：经M01/M11事务端口写入。每个结果显式 immediate/onSuccessfulExtract/runOnly；死亡与放弃不能把撤离条件实物变永久，也不能清除永久结局。R0经济规则未决、R2-b/c未接通时，R6-b不能假称完成；可先做隔离多局验收，不把替身当真实跨局集成。

设计评审可考察一个更小的独立接入切片：环境线索的“已阅读/已做选择”若明确属于immediate永久故事记录，且完全不发物品、不扣稀有卡、不改撤离结算，可通过现有M01基地单次提交实现。需先列实际事件实例ID和同一事件幂等键、重载/下一局识别方式，证明不会凭重开连领或重复推进，再由Friday批准；不能仅因经济包待定就把所有故事接入都判定不可能。凡涉及物资代价、成功撤离奖励或跨键原子性仍遵守R0/R2门，不走旁路。

范围：新增story模块、Ink源与正规编译产物、定向测试；经根审批准的最小事件/基地纪念物接线。不改人物bible、职业收藏规则、旧事件卡效果或地图层数。

## 验收

正常：两条完整路径分别经历三段、跨局推进，最终至少一项地图条件或基地纪念物真实不同；文案提前说明关键后果。

失败：未知事件/选项和未达条件拒绝且无状态变化；Ink加载失败可返回原流程；坏故事存档保留原数据并可诊断，不默认授予结局。

重试：每段刷新和重复点击收据一致，无重复奖励；成功撤离/死亡/放弃分别按后果矩阵；临时标记不串下一局。

停止点：先审文本/后果矩阵，再实现独立模块；最后真实多局IAB验收。制作人剧情认可单列，不延伸全篇主线。

根接线预读：普通事件入口实际为 `game.run.flow.js/resolveCell` 的event分支→`runEventDeck`；它已有一次性visited守卫、页面返回恢复及Ink异常回退。R6最小环境事件接入应在该真实链路内挑合格事件槽，保留普通随机事件回退，不改十张现有事件卡效果。故事提交失败前不得提前consumeCurrentCell，成功故事记录与节点已消费分两次保存时，刷新须凭永久同事件收据重开结果页/补消费，而不能重复推进下一段；R2未闭环不能声称跨键原子。

R6只读预研根审修订（尚未授权实现）：候选沿用教学楼/体育馆/校工值班室/北门环境，不造人物背景。原“修复引路灯 vs 拿现成路线折页”缺乏互斥理由，必须重写成明确有限场景资源或不可逆路径的选择；不能仅靠按钮二选一强制一个本可两做的假取舍。仅叙事场景资源，不扣玩家物资；纪念及后续描写不同，经济不接入。

幂等补充：只把本局已读写在run里不足。Base提交成功但run未保存时，重载可能回前一格，换另一个事件节点会形成新instance并错误推进同局第二段。Base永久故事状态亦需保存稳定run身份的一段上限，回滚/换节点时仍识别。R2已有私有_r2.runId，只是RunStorage公开读取剥除了它；后续评估最小只读身份接口，不凭空声称没有runID。若仅用mapSeed必须写清碰撞/旧档局限，不把种子当天然全球唯一身份。

有限环境稿由Friday代制作人先审实现，不额外等待审美答复才能做原型；最终剧情认可仍单列。当前预研Sol已停止，没有R6运行时修改。

2026-09-21 05:35 根批准R6-A-LAST-LAMP-DESIGN文本和最小接线，派 `/root/sol_r3_battle` 唯一实施。稳定身份必须取session真实activeSlot，不借开发局的旧Base.slot；只读identity须校验版本/字段/待恢复日志。Base story强schema及跨字段一致性，保存失败明确提示并可重试；Ink失败不因故事消费，原普通事件依旧可正常完成。只读基地纪念明确无地图或战力效果。根实际验收之前不算通过。
