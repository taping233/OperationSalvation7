# R3-0 命令交互守卫

玩家可见变化：双镖第二次目标选择、背包砸击只在当前合法选择中生效；快速重复点击、取消后迟到点击、目标已死亡不能凭空再次伤害或改打另一敌人。保留卡牌数值和普通/首脑差异。

输入：现有Battle.commands.resolveDart/resolveSlam参数、当前interaction及活目标、能量。输出：有效操作沿当前规则结算一次；无对应pending时拒绝，不变能量/目标状态；无效目标保留待选状态以便重选，取消则清理。禁止新建第二结算器。

文件范围：battle.core.js仅resolveDart/resolveSlam及必要相邻交互接线；新增针对真实commands的回归tests；本包交付文档。已有闪避/effect-steps/卡库半成品不动。不改预览或动画/存档；本包先把命令约束做对。

验收：先用真实命令测试复现无pending调用、有效后重复、取消后调用、死目标/错误side，然后最小修复。合法双镖仍可选择另一目标或同一目标；非法调用不消耗、无随机目标回退；砸击合法只扣2能量一次，能量不足保留可理解的选择/错误状态。普通/首脑至少各一组边界，结束后可继续出牌。双镖首段击杀不把流血转给另一人同样核验，有问题再在同小包最小修复。

根IAB将用游戏正常入口/现有开发者工具验收；已有节点测试局不写玩家档。程序测试不能取代这一步。停止点：交真实改动、定向测试、未验收项给Friday；R3-a由根验收后另派。
