# M01—M06 本批成果与验收

日期：2026-09-21。Sol 子代理实现，Friday 集成与 Codex 内置浏览器验收。

> 此文保留第一批交付快照，不能作为当前完成度证明。随后 R0 发现职业卡带入/带出方向与老板规则冲突，M06 旧资格预览尚待规则迁移；禁止据此宣称职业卡经济已验收。R1 已进一步完成房间候选背景接入和 1280×720/1920×1080 实机布局，R2-a 已做跨键恢复基础但尚未接出征结算。最新进展与档位05状态以 `LINEAR-EXECUTION-2026-09-21.md` 末尾为准。

## 交付结论

交付的是可操作的基地养成原型及其业务模块：购买家具、布置房间、永久收藏展示、收藏目标追踪、两套整备预设已经接入游戏。尚不是最终美术版，也不代表十二个模块全部完成。

| 模块 | 实际交付 | 保留边界 |
| --- | --- | --- |
| M01 存档 | 只读快照、修订号检查、持久请求收据、单基地键失败不更新内存；新增家具/外观/目标状态 | 跨键事务属于 M11，未改造所有旧式写档 |
| M02 收藏与人物 | 消耗实体卡、保留永久记录、对应人物经验、皮肤选择，接入旧界面 | 奖励保留原领取入口，无新增剧情或皮肤原画 |
| M03 房间业务 | 8×6 房间、6 种家具、购买/摆放校验、布局保存、2 个收藏展位 | 价格为原型数值，经济平衡未验收 |
| M04-a 视觉方向 | 基地/收藏/整备/战斗提示四张界面样张，共享资源清单和锚点 | 新房间与家具是 SVG 几何原型，无 NovelAI 正式美术，风格待认可 |
| M05 基地场景 | Pixi 房间家具、静态人物、实际收藏卡面、5 热点与旧入口 | 人物复用静态插画，无骨骼或待机动画，观感需要正式资产替换 |
| M06 下一局准备 | 最多 3 个追踪目标、来源说明、两套可编辑预设、库存资格预览、应用卡牌到原整备页 | 人物/宠物未自动开局，直接出征事务待 M11 |

## 试用入口

游戏：`http://127.0.0.1:4179/`，进入档位 05。可以看到座椅、飞刃偷袭永久收藏展位；家具工坊可购买布置；出征页的“下一局目标与两套预设”可追踪目标、改名及应用方案。

四张方向样张：`docs/previews/m04/index.html`。Vite URL 为 `http://127.0.0.1:4179/@fs/D:/素材/代号柒/搜打撤/docs/previews/m04/index.html`。普通 `/docs/` 路径会回退到游戏入口；实际游戏使用根路径，不能用 `/game/` 作为素材验收地址。

档位 05 是受控验收档，初始设为 20 储备币、2 张当前卡库的飞刃偷袭。准备页仅在该档为空时允许建立，拒绝覆盖。购买、收藏、摆放、展示均在实际游戏界面执行；不是自然游玩赚币或完整战斗的证据。

## 根任务实机证据

- 购买座椅：20 → 17 储备币，获得 chair-1。
- 保存摆放：room-floor 的 (3, 2)、旋转 0；刷新重进后保留。
- 两张飞刃偷袭全部收藏：实体库存 2 → 0，永久记录保留，人物“无”获得 20 经验。
- 左展位绑定永久记录：库存为空仍显示卡面，刷新重进后保留。
- 追踪飞刃偷袭：1/3，重进后保留，显示来源说明。
- 方案一通过真实键盘输入改名为 QA：已写入实际存档，重新预览保留；应用空合法方案进入原整备页，仍需原确认出发。
- 只读比对确认：根任务验收期间档位 01—04 原始存储未变化。
- 实际 DOM 视口 1158×986：无横向溢出、无损坏图片。尝试设置 1280×720 和 1920×1080 后 DOM 尺寸未变化，故不算这两个尺寸的独立实机通过，已恢复视口设置。

## 集成修复

1. 场景错误地从已隐藏的快照内部字段取 revision，导致有币也买不了家具。现改为传递 readBase 的公开 revision，加入真实读档→场景投影→购买回归，并完成实机购买。
2. 预设读模型用 id，保存命令要求 presetId。现修复名称/人物/宠物和卡牌清单保存的字段映射，真实改名写档通过。
3. 去掉 M11 缺席时必然失败的“使用此方案出征”按钮；明确应用卡牌后在旧整备页确认宠物，人物在出发后选择。

## 自动验证

最终定向回归：9 文件、37 项全部通过，覆盖本批 M01—M06 新增测试。

```text
npx vitest run tests/m05-home-presenter.test.js tests/m05-home-scene.test.js tests/m01-base-commit.test.js tests/m02-collection-commands.test.js tests/m03-home.test.js tests/m06-preparation.test.js tests/m06-sources-view.test.js tests/m04-visual-pack.test.js tests/m04-preview-contract.test.js --no-file-parallelism
```

未重跑全量测试、生产 build、EXE 打包、完整战斗流程；既有 collection-room、architecture 历史失败未混入本批。WebGL 故障回退有模块测试，未真实关闭 WebGL 复验。

## 未完成的原契约项

- M04 正式风格认可及正式资源制作。
- M05 在 1280×720、1920×1080 的独立实机布局验收。
- M01/M06 依赖 M11 的跨键事务和重启幂等开局。
- M07—M12 不属于本批实现范围。

本批可交付试用审阅，但不能把六个完整契约全部标成最终验收通过。下一步应先评审基地体验和视觉方向，再推进正式视觉制作。
