# 项目约束

- 验证与检测流程应优先使用 Codex 内置服务器或内置工具；仅当内置能力无法完成必要验证时，才尽量少量使用外部浏览器。
- Godot 版已冻结，不再作为当前开发主线；项目当前主线为 Vite 版。
- 接手本项目必读交接文档：`搜打撤/docs/NEXT-AI-HANDOFF-2026-09-20.md`。先读其中带复核时间的“当前状态”；标注 2026-09-20 的盘点与清单是历史快照，实时工作区以 `git status --short` 为准。
- 工作区提交、改动归属或验证状态变化时，更新交接文档“当前状态”的复核时间、HEAD/远端差异、改动清单和证据；归属或验收不明就写“未核实”，不得按文件名推断。历史快照加日期保留，不覆盖成当前事实。

## 省 token 工作口径（2026-09-22 老板令固化）

1. 读文件：>300 行禁止整读——先 grep 定位行号再 offset/limit 读片段。
   battle.view.js / cards.js / game.hub.js 等千行文件尤其如此。
2. 跑测试：默认只跑改动相关用例 `npx vitest run <关键词>`，例如：
   - 改 combat.js → `vitest run combat effect-verbs battle`
   - 改 cards.js → `vitest run cards hero-cards card-art-coverage snapshot-guard`
   - 改 CSS/纯文案/纯图片 → 不跑测试，实机截图验收
   全量 `vitest run --no-file-parallelism` 仅限：收尾落库前，或改了
   effect-steps/architecture/公共层。输出加 --reporter=dot 或 head/tail 截取，
   不吞全量堆栈。
3. git：status/diff 一律 --short；看改动用 diff --stat 或指定文件，
   禁全仓裸 diff（core.quotepath false 已配置，消中文转义）。
4. 先 grep 再读；已查证的事不重复验证；结论绑定行号/哈希不复述过程。

## 修改与交付边界

- 只改老板要求的范围；能从代码、文件或日志查清的先查，玩法/产品决策无法推断时再询问。
- 任何删除（含未跟踪、临时文件和死代码）先列明路径并获老板同意；不用 `git clean` 或 `git checkout` 清理现场。回滚、提交、推送和对外发布也须按老板明确指示执行。
- 改动前核对并行任务的文件所有权；其他会话的未提交改动在场时，不 build、不覆盖共享文件、不顺手提交。
- 只开发 Vite 主线；Godot、Unity 和桌面 EXE 均冻结。测试通过、生成成功或代码落地不自动等于完整验收；汇报时区分定向验证、全量验证和真实页面验收。
- 接手状态只能作为带时间的快照；即使复核时间很近，开始操作前也重跑 `git status --short` 和 `git log -1`。

## 对话执行默认值

- 单问题直接回答，不扫描仓库；需要当前状态或证据时才查文件/工具。
- 小改只读目标代码及必要上下文，限于请求范围，并做对应验证。
- 大任务先盘点、排优先级，再逐切片实现；不默认全仓审计或一次铺开所有模块。
- 同一对话已经核实的背景直接沿用；代码、工作区状态或时效性变化时再核对。
- 汇报只列改动、验证和未完成项；长任务仅在有实质进展、阻塞或决策点时更新。
- 默认自行处理可逆的实现细节；删除、回滚、提交、外部写入和未定义玩法规则按项目约定处理。
- 可用输入格式：`目标：…；范围：…；验收：…`。缺项时按最窄安全范围推进，只在关键决策无法推断时提问。
