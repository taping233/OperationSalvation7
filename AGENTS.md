# 项目约束

- 当前只开发 `搜打撤` Vite 版；Godot、Unity 和桌面 EXE 冻结。
- 单问题直接回答；小改只读目标文件及必要上下文，大任务先盘点再逐切片实现。已核实的背景不重复查；超过 300 行的文件先 `rg` 定位再读片段。
- 开始改动前查 `git status --short` 和 `git log -1`，核对并行任务的文件所有权；其他会话有未提交改动时，不覆盖共享文件、不 build、不顺手提交。只看相关文件的 diff 或 `--stat`，不输出全仓 diff。
- `搜打撤/docs/NEXT-AI-HANDOFF-2026-09-20.md` 是带时间的交接记录，仅在接手开发、判断改动归属或交付状态时按相关章节读取；历史快照不是当前事实，以实时工作区和代码为准。提交、改动归属或验证状态变化时，更新其当前状态的时间、HEAD/远端差异、改动及证据；不明项写“未核实”，保留历史记录。
- 默认只跑相关测试。CSS、纯文案和纯图片改动做实际页面/图片检查；收尾落库前或改公共架构时才运行 `npx vitest run --no-file-parallelism`。优先用 Codex 内置工具和浏览器验证；区分定向验证、全量验证与真实页面验收。
- 只改老板要求的范围；玩法决策无法从代码和文档确认时再问。任何删除（含未跟踪文件、临时文件和死代码）须先列路径并获同意；回滚、提交、推送、发布和外部写入也须老板明确指示，不用 `git clean` 或 `git checkout` 清理现场。
- 汇报只列改动、验证和未完成项；测试或生成成功不等于完整验收。
